generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  id            String    @id @default(cuid())
  name          String
  email         String    @unique
  password      String
  knownPassword String?
  role          String    @default("user")
  loginAttempts Int       @default(0)
  lockedUntil   DateTime?
  sessionToken  String?   @unique
  tokenExpiresAt DateTime?
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  passwordResetRequests PasswordResetRequest[]
}

model Post {
  id        String   @id @default(cuid())
  title     String
  content   String?
  published Boolean  @default(false)
  authorId  String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model PasswordResetRequest {
  id                   String    @id @default(cuid())
  userId               String
  user                 User      @relation(fields: [userId], references: [id])
  status               String    @default("pending") // pending, approved, rejected
  newGeneratedPassword String
  oldPassword          String?
  desiredPassword      String?
  reason               String?
  createdAt            DateTime  @default(now())
  resolvedAt           DateTime?
  resolvedBy           String?
}

model DashboardData {
  id                    String   @id @default("singleton")
  data                  String
  totalRecords          Int      @default(0)
  excelHeadersByOrigin  String   @default("{}")
  updatedAt             DateTime @updatedAt
}

model AuditLog {
  id        String   @id @default(cuid())
  action    String   // login_success, login_failed, login_blocked, password_request, password_approved, password_rejected, user_unlocked, user_locked, logout, password_request_expired
  userId    String?
  userEmail String?
  userName  String?
  ip        String?
  details   String?
  createdAt DateTime @default(now())
}
